<?xml version="1.0" encoding="UTF-8"?>
<entityConf xsi:noNamespaceSchemaLocation="../../entityconf.xsd"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" backend="com.ant.crawler.core.ListSiteCrawler">

	<entityFields>
		<rssSite>
		</rssSite>
		<listSite>
			<itemXpath>//ul[@class='clearfix listmovie']/li</itemXpath>
			<field xpath="a/@href" name="tempSource">
				<filter type="scriptone">
					<![CDATA[
						var URL = Java.type("java.net.URL");
						var url = new URL(entity.getSourceUrl(), field);
						entity.setDetailUrl(url);
					]]>
				</filter>
			</field>
			<field xpath="a/div/p/text()" name="title"></field>
		</listSite>
		<detailSite>
			<field xpath="//div[@class='box-info']/div[@class='publish']/a[@class='hd']" name="filterQuality"></field>
			<field xpath="//div[@class='noi-dung-phim']/h1[2]/*" name="description" required="false"></field>
			<field xpath="//div[@class='box-info']/div[@class='publish']/a[position() > 1]/text()" name="tags"  required="false">
				<filter type="regexone" replace="">Phim </filter>
				<filter type="regexall" replace=", ">\n</filter>
			</field>
			<field xpath="//div[@class='fiml-img']/img/@src" name="thumbnail">
				<filter type="scriptone">
					<![CDATA[
						var URL = Java.type("java.net.URL");
						var url = new URL(entity.getSourceUrl(), field);
						field= url.toString();
						var Date = Java.type("java.util.Date");
						entity.set("create_date", new Date());
						entity.set("owner_email", "tronglongcntt@gmail.com");
						entity.set("parrent", 0);
						entity.set("view", java.util.concurrent.ThreadLocalRandom.current().nextInt(40, 60));
					]]>
				</filter>
			</field>
			<field xpath="//object[@id='flashplayer']/embed/@flashvars" name="embed_web">
				<filter type="scriptone">
					<![CDATA[
					try {
						//find link
						var proxyPrefix= "proxy.link=";
						var index = field.indexOf(proxyPrefix);
						if (index == -1) {
							field = null;
							return;
						}
						index += proxyPrefix.length();
						var end = field.indexOf("&", index);
						field = field.substring(index, end);
						
						var encoder = java.util.Base64.getUrlEncoder();
						var IOUtils = Java.type("org.apache.commons.io.IOUtils");
						var URL = Java.type("java.net.URL");
						var state = IOUtils.toString(new URL("http://phimdb.net/get_picasa/0/" + encoder.encodeToString(field.getBytes()).replace('_', '/')));
						java.lang.System.out.println(state);
						if (state.length() < 10) {
							field = null;
						}
					} catch (e) {
						//e.printStackTrace();
						field = null;
					}
					]]>
				</filter>
			</field>
		</detailSite>
	</entityFields>
	<categories mappingField="category_id">
		<category id="1">
			http://phimhd3s.net/phimle-1.html
		</category>
	</categories>
</entityConf>